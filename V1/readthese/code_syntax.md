This will show the format of how you should code, so that the style of code remains consistent:

Use Spaces in Between Operators:
foo = bar;

Put curly brace on same line:
if (foo){
    bar();
}

All user-created names use underscores
int foo_bar;